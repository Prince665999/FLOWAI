import { useEffect,useState } from "react"; import { ActivityIndicator,FlatList,Pressable,Text,View } from "react-native"; import { router } from "expo-router"; import { listOrders } from "../../../src/api/orders";
export default function OrdersScreen(){const [orders,setOrders]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{listOrders().then(setOrders).finally(()=>setLoading(false));},[]);if(loading)return <ActivityIndicator/>;return <View style={{padding:16}}><Text style={{fontSize:24,fontWeight:"700"}}>Orders</Text><FlatList data={orders} keyExtractor={x=>String(x.id)} renderItem={({item})=><Pressable onPress={()=>router.push(`/(dashboard)/orders/${item.id}`)} style={{padding:14,borderBottomWidth:1,borderColor:"#ddd"}}><Text>{item.order_number}</Text><Text>{item.status} · {item.payment_status}</Text></Pressable>}/></View>}
