export default function ProductSpecs({ specifications }: { specifications: Record<string, unknown> }) { const entries=Object.entries(specifications); return <section><h2 className="font-semibold">Specifications</h2>{entries.length ? <dl className="mt-3 divide-y rounded-lg border">{entries.map(([key,value])=><div className="flex justify-between p-3" key={key}><dt className="text-slate-500">{key}</dt><dd>{String(value)}</dd></div>)}</dl> : <p className="mt-2 text-slate-500">No specifications listed.</p>}</section>; }
